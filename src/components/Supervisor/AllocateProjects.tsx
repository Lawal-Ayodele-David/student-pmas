import React from 'react';
import ProjectList from '../common/ProjectList';

const AllocateProjects: React.FC = () => {
  return (
    <div>
      <h2>Allocate Projects</h2>
      <ProjectList />
    </div>
  );
};

export default AllocateProjects;
